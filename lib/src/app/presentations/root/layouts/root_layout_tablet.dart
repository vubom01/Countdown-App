import 'package:flutter/material.dart';

class RootLayoutTablet extends StatelessWidget {
  const RootLayoutTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
